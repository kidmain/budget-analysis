# Analysis Parameters
BUDGET_YEARS=2023,2024,2025
BUDGET_TARGET_EXPENSE=65000
BUDGET_MAX_EXPENSE=70000
BUDGET_TARGET_DIFF=30000
BUDGET_MAX_DIFF=50000
BUDGET_TARGET_PASSIVE_INCOME=100000

# Financial Targets (%)
BUDGET_SAVING_RATE_TARGET=30
BUDGET_EXPENSE_RATIO_TARGET=70
BUDGET_ASSIST_RATIO_TARGET=10
BUDGET_DISPOSABLE_RATIO_TARGET=80
BUDGET_BURN_RATE_TARGET=4

# Database Configuration
DB_NAME=budget_db
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432

# Database Connection String
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Database Pool Settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30

# Application Settings
DEBUG=False
LOG_LEVEL=INFO
TIMEZONE=UTC 